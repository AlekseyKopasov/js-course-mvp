export interface Lecture {
  id: string;
  title: string;
  content: string;
  order: number;
}

export interface LectureMetadata {
  id: string;
  title: string;
  order: number;
}

export interface LectureInfo {
  id: string;
  title: string;
  order: number;
}
